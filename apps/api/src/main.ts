import dotenv from "dotenv"; dotenv.config({path:"../../.env"});import {ValidationPipe} from"@nestjs/common";import{NestFactory}from"@nestjs/core";import{AppModule}from"./app.module";
async function bootstrap(){const app=await NestFactory.create(AppModule,{cors:true});app.setGlobalPrefix("api");app.enableCors({origin:true,credentials:true});app.useGlobalPipes(new ValidationPipe({whitelist:true,transform:true,forbidNonWhitelisted:false}));await app.listen(Number(process.env.PORT??4000));console.log(`THESIS API listening on http://localhost:${process.env.PORT??4000}/api`)}void bootstrap();
