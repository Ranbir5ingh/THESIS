"use client";import Link from"next/link";import{useEffect,useState}from"react";import{ArrowRight,BrainCircuit,Clock3,RefreshCcw}from"lucide-react";import{motion}from"motion/react";import{api}from"@/lib/api";import{Card}from"@/components/ui/card";import{Button}from"@/components/ui/button";import{ScoreRing}from"@/components/ui/primitives";
export default function MyThesis(){const[data,setData]=useState<any[]>([]);useEffect(()=>{api<any[]>('/thesis').then(setData).catch(()=>{})},[]);return <div className="space-y-8"><div><p className="section-kicker">Your investment journal</p><h1 className="mt-3 text-4xl font-black tracking-tight sm:text-6xl">My <span className="text-[var(--accent)]">thesis.</span></h1><p className="mt-4 max-w-2xl text-[var(--muted)]">The point is not to be right every time. It is to make your reasoning visible enough to revisit.</p></div>{data.length?<div className="grid gap-4 md:grid-cols-2">{data.map((x,i)=><motion.div key={x.id} initial={{opacity:0,y:15}} animate={{opacity:1,y:0}} transition={{delay:i*.05}}><Card className="glass-hover p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-xs text-[var(--muted)]">{x.symbol} · {x.name}</p><p className="mt-3 line-clamp-3 text-lg font-semibold leading-7">“{x.thesis_text}”</p></div><ScoreRing value={x.thesis_score} size={70}/></div><div className="mt-6 flex items-center justify-between border-t border-[var(--line)] pt-4"><div className="flex items-center gap-2 text-xs text-[var(--muted)]"><Clock3 size={14}/>{new Date(x.created_at).toLocaleDateString()}</div><Link href={`/thesis/${x.symbol}`} className="text-xs font-semibold text-[var(--accent)]">Re-evaluate <ArrowRight className="ml-1 inline" size={13}/></Link></div></Card></motion.div>)}</div>:<Card className="grid min-h-[420px] place-items-center p-8 text-center"><div><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-[var(--accent-soft)] text-[var(--accent)]"><BrainCircuit/></div><h2 className="mt-5 text-2xl font-bold">No thesis saved yet.</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-[var(--muted)]">Pick an asset, write why you believe in it, and let THESIS challenge the weakest part of your reasoning.</p><Link href="/explore" className="mt-6 inline-flex"><Button>Find an investment <ArrowRight size={16}/></Button></Link></div></Card>}</div>}
