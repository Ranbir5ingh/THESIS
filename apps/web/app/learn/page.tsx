import { BookOpen, BrainCircuit, CircleDollarSign, Gauge, Scale } from "lucide-react";
import { Card } from "@/components/ui/card";
const groups=[['START HERE',BookOpen,['What is a stock?','What is an ETF?','What is market capitalization?']],['UNDERSTAND METRICS',CircleDollarSign,['P/E','P/B','ROE','Debt / Equity','EPS','Revenue growth']],['UNDERSTAND RISK',Gauge,['Volatility','Drawdown','Diversification','Risk tolerance']],['BEHAVIOR',BrainCircuit,['FOMO','Herding','Confirmation bias','Loss aversion']]];
export default function Learn(){return <div><p className="text-xs uppercase tracking-[.18em] text-[var(--muted)]">Financial literacy</p><h1 className="mt-2 text-4xl font-black">Learn without the textbook.</h1><p className="mt-3 max-w-2xl text-[var(--muted)]">Short, contextual lessons that explain what a metric means—and why it matters when you actually make a decision.</p><div className="mt-8 grid gap-4 md:grid-cols-2">{groups.map(([title,Icon,items])=>{const I=Icon as any;return <Card key={title as string} className="p-6"><I className="text-[var(--accent)]"/><h2 className="mt-5 font-bold">{title as string}</h2><div className="mt-4 grid gap-2">{(items as string[]).map(x=><div key={x} className="rounded-xl border border-[var(--line)] bg-white/[.02] p-3 text-sm">{x}<span className="float-right text-[var(--muted)]">2 min</span></div>)}</div></Card>})}</div><Card className="mt-4 p-6"><div className="flex gap-3"><Scale className="text-[var(--accent)]"/><div><h2 className="font-bold">Why a good company can be a bad investment</h2><p className="mt-2 text-sm leading-6 text-[var(--muted)]">Because price matters. A wonderful business can still be priced so aggressively that future returns disappoint. THESIS separates asset quality from the price you pay and your personal fit.</p></div></div></Card></div>}
